# ===== 実患者DICOM由来は絶対にgitに載せない（PHI保護）=====
real_local/
*.npy
*.dcm
*.csv
# 描画画像（一部は患者CT由来＝PHI）。.py から再生成可能
*.png
*.gif
*.jpg
*.jpeg
*.mp4
# DB/インベントリのダンプ（患者情報を含み得る）
inventory.json
vol_meta.json
*_list.json
path_clicks.json

# 外部の参照用クローンは git 管理外
_vendor/
# ビルド成果物
build/
DerivedData/
# 配布キット(署名済みバイナリ等)
dist/
*.zip

# misc
.DS_Store
__pycache__/
*.pyc
.venv/
~$*

# Local distribution scripts (may contain personal cloud paths) — never committed
app/distribute_local.sh
*_local.sh

# クリック起動アプリ（ユーザー固有のパスを含む・配布はPyInstallerで別途）
*.app/

# brandアイコン資産（PHIではない）はコミットする
!app/icon_1024.png
!app/icon.icns
!app/icon.ico
!app/docs/bmc_qr.png

# サンプルCT(TCIA HCC-TACE-Seg, CC BY 4.0の公開データセット。患者の実データではない)
# 出典・ライセンス表記は sample_data/ATTRIBUTION.md 参照
!sample_data/
!sample_data/**
!sample_data/**/*.dcm
